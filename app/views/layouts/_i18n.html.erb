<ul>
  <% I18n.available_locales.each do |l|%>
    <li><%= link_to l, url_for(locale: l.name), class:"font-medium tracking-wide text-gray-700 transition-colors duration-200 hover:text-deep-purple-accent-400" %></li>
  <% end %>
</ul>